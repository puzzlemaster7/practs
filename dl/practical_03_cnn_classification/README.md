# Practical 3: Classification with Neural Networks

## Full question (as given)
**Convolutional Neural Networks (Classification) (OC2, OC3, OC4)**  
a. Implementing deep neural network for performing binary classification task  
b. Using a deep feed-forward network with two hidden layers for performing multiclass classification and predicting the class.

This practical has two sub-practicals:
- `a_binary_classification/` (binary classification)
- `b_multiclass_classification/` (multiclass classification)

Run each sub-practical independently by following its own `README.md`.

## Suggested improvements (optional)
- Rename the practical folder to reflect it uses dense networks (not CNN) for these tasks.

## Folder structure
1) `a_binary_classification/`
2) `b_multiclass_classification/`
