# Optional offline dataset (CSV)

If you want to use real stock prices *without internet*, place a CSV here and modify `main.py` to load it.

Recommended columns:
- `date` (any format)
- `close` (float)

Keep the file name simple, e.g. `google_stock.csv`.

