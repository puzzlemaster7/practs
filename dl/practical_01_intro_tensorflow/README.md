# Practical 1: Introduction to TensorFlow

## Full question (as given)
**Introduction to TensorFlow (OC1)**  
a.  
- Create tensors with different shapes and data types.  
- Perform basic operations like addition, subtraction, multiplication, and division on tensors.  
- Reshape, slice, and index tensors to extract specific elements or sections.  
- Performing matrix multiplication and finding eigenvectors and eigenvalues using TensorFlow  
b. Program to solve the XOR problem.

This practical has two sub-practicals:
- `a_tensor_basics/` (tensor ops + reshape/slice/index + matrix multiply + eigenvalues/eigenvectors)
- `b_xor/` (solve XOR using a small neural network)

Run each sub-practical independently by following its own `README.md`.

## Suggested improvements (optional)
- Add a `run_all.ps1` and `run_all.sh` to execute both parts in one go.

## Folder structure
1) `a_tensor_basics/`
2) `b_xor/`
